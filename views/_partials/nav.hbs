<nav class="navbar navbar-expand-lg navbar-light bg-light shadow">
  <a href="/">
    <img src="../../public/img/udemy-logo.svg" alt="Udemy" width="110" height="32" class="mr-3">
  </a>

  <div class="collapse navbar-collapse">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <button class="dropdown-toggle mr-3" data-toggle="dropdown">Categories</button>
        <ul class="dropdown-menu">
          {{#each category}}
          <li><a class="dropdown-item" href="/category/{{id}}"> {{name}} &raquo </a>
            <ul class="submenu dropdown-menu">
              {{#each subCat}}
              <li><a class="dropdown-item" href="/category/{{id}}"> {{name}} </a></li>
              {{/each}}
            </ul>
          </li>
          {{/each}}
        </ul>
      </li>
    </ul>
    <form id="frmSearch" class="d-flex flex-row input-group {{#if isAuth}}mr-3{{/if}}">
      <input class="form-control" type="search" placeholder="Search for anything" aria-label="Search">
      <button class="btn btn-outline-success" type="submit">
        <i class="fas fa-search"></i>
      </button>
    </form>
    {{#if isAuth}}
    <a href="/user/cart" style="padding-left: 5px; padding-right: 5px">
      <i class="fas fa-shopping-cart pr-2 pl-1"></i>
    </a>
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <button class="dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true"
          aria-expanded="false">HI, <img src="{{#if picture}}{{picture}}{{else}} ../img/ava_default.jpg{{/if}}"
            alt="Avatar">
          <b>{{name}}</b></button>
        <div class="dropdown-menu dropdown-menu-right">
          <a class="dropdown-item" href="/user/profile"><i class="fas fa-user pr-3"></i> Account</a>
          <a class="dropdown-item" href="#"><i class="fas fa-book-open pr-3"></i>My Courses</a>
          <a class="dropdown-item" href="/user/sign-out"><i class="fas fa-sign-out-alt pr-3"></i>Log out</a>
        </div>
      </li>
    </ul>
    {{else}}
    <a href="/lecturer" class="nav-link text-nowrap">Teaching on Udemy</a>
    <a href="{{#if forUser}}/user/log-in{{else if forLecturer}}/lecturer/log-in{{else}}/admin/log-in {{/if}}"
      class="btn btn-outline-primary mr-2 text-nowrap">Log in</a>
    {{#if forAdmin}}

    {{else}}
    <a href="{{#if forUser}}/user/sign-up{{else if forLecturer}}/lecturer/sign-up{{else}}/admin/sign-up {{/if}}"
      class="btn btn-primary text-nowrap">Sign up</a>
    {{/if}}
    {{/if}}
  </div>
</nav>